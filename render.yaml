services:
  - type: web
    name: insurance-fraud-engine
    runtime: python
    plan: free
    buildCommand: pip install fastapi uvicorn jinja2 python-multipart pandas numpy scikit-learn xgboost shap matplotlib seaborn reportlab
    startCommand: uvicorn web.main:app --host 0.0.0.0 --port $PORT
